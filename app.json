{
    "name": "lingua",
    "description": "Lingua translation server",
    "repository": "https://github.com/survos-sites/translation-server",
    "scripts": {
        "dokku": {
            "predeploy": "./c mess:stop && secrets:decrypt-to-local --force && ./c importmap:install && ./c asset-map:compile && ./c fos:js-routing:dump --format=js --target=public/js/fos_js_routes.js --callback=\"export default \" && ./c doctrine:migrations:migrate -n --allow-no-migration",
            "postdeploy": "php bin/console about"
        }
    },
    "healthchecks": {
        "web": [
            {
                "type": "startup",
                "name": "web check",
                "path": "/health",
                "attempts": 3,
                "wait": 2,
                "timeout": 5
            }
        ]
    }
}
